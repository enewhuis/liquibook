project : liquibook, liquibook_test, boost_unit_test_framework, boost_base {
  libs += gtest
  libs += gtest_main
  includes += $(GTEST_ROOT)/include
  libpaths += /usr/local/lib
  libpaths += $(GTEST_ROOT)/lib/.libs
}
